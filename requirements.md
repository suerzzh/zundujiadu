# novel2script — 需求文档

**版本：** 0.1  
**日期：** 2026-06-05  
**状态：** 待评审

---

## 1. 背景与动机 

大量网络小说、原创小说作者希望将自己的作品改编成影视剧本，但存在两道门槛：

1. **专业门槛**：影视剧本格式（场景标题、动作描述、对白格式、转场等）与小说写法截然不同，作者需要专业培训才能上手。
2. **效率门槛**：即便了解格式，手工逐章改写极耗时间，导致大量小说版权无法被有效开发。

novel2script 的目标是用 AI 打通"小说 → 影视剧本初稿"这一转换链路，让作者快速获得**可编辑、可进一步打磨**的剧本初稿，而不是一份"一次性"的机器输出。

---

## 2. 目标用户

- 希望改编自己小说的原创作者（主力用户）
- 制片公司或剧本编辑，需要快速评估小说的剧本改编潜力
- 开发者，需要将此能力集成进更大的写作/内容平台

---

## 3. 功能需求

### 3.1 核心功能

| 编号 | 需求描述 | 优先级 |
|------|----------|--------|
| F-01 | 接受包含 3 章以上内容的中文小说文本，自动转换为结构化影视剧本（YAML 格式） | Must |
| F-02 | 支持单个 txt 文件输入，通过识别章节标题自动切分章节 | Must |
| F-03 | 支持文件夹输入，每个文件视为一章（按文件名排序） | Must |
| F-04 | 输出符合本项目 YAML Schema 规范的剧本文件 | Must |
| F-05 | 提供 CLI 命令行界面，指定输入文件/目录，指定输出路径 | Must |
| F-06 | 场景（scene）严格按照时空变化切分，不跨时空合并场 | Must |
| F-07 | 全局场号连续编号，贯穿整部剧本 | Must |
| F-08 | 第一阶段提取全书"剧本圣经"（角色表 + 主要地点），后续各章转换时注入该上下文，保证角色称呼和地点名称一致性 | Must |
| F-09 | 每场包含：场号、场景标题（slugline）、动作描述、对白序列、出场角色、转场提示（可选）、本场梗概、来源章节 | Must |
| F-10 | 顶层输出包含剧本元信息（剧名、简介、题材、生成时间、来源）及全局角色表 | Must |
| F-11 | 转换过程中显示进度（当前处理第几章/共几章） | Should |
| F-12 | 支持通过命令行参数或配置文件指定 DeepSeek API Key 和模型名称 | Must | 

### 3.2 输出规范

- 输出格式：UTF-8 编码的 YAML 文件（`.yaml`）
- Schema 版本字段：`schema_version: "1.0"`
- 每一场包含 `source_chapter` 字段，标注来自小说第几章，方便作者对照原文

### 3.3 错误处理

| 场景 | 处理方式 |
|------|----------|
| 章节数不足 3 章 | 输出警告并继续，不阻断执行 |
| 某章 API 调用失败 | 重试 3 次，失败后在该章位置写入 `error` 占位场，继续处理后续章节 |
| 输出 YAML 不符合 Schema | 输出警告并保存原始响应供调试 |
| 章节标题识别失败 | 将整个文件视为单一章节并给出提示 |

---

## 4. 非功能需求

| 编号 | 需求描述 |
|------|----------|
| NF-01 | 对于 10 章、每章 3000 字的小说，端到端转换时间不超过 5 分钟（取决于 API 响应速度） |
| NF-02 | 单次 API 调用失败不影响整个转换任务（章节级容错） |
| NF-03 | 代码模块化，核心转换逻辑（不含 CLI）可被 Web 应用复用 |
| NF-04 | 所有提示词（prompt）以独立文件维护，方便在不动代码的前提下调优 |
| NF-05 | 输出 YAML 格式对人类可读：字符串不加不必要的引号，列表缩进清晰 |

---

## 5. 约束与边界

- **语言**：Python 3.10+
- **AI 后端**：DeepSeek API（兼容 OpenAI SDK 格式）
- **本版本范围**：CLI 工具，不含 Web UI（Web UI 为后续迭代）
- **剧本类型**：影视剧本（电影/电视剧），不含舞台剧、短剧、广播剧
- **输入语言**：中文小说（英文输入不在本版本测试范围内）

---

## 6. 不在本版本范围内（Out of Scope）

- Web UI / API 服务
- 剧本编辑器（输出可编辑 YAML 文件，供作者在任意编辑器中修改）
- 自动分集分幕（当前输出扁平场列表）
- PDF / Final Draft 格式导出
- 多语言输入支持
- 用户账号系统

---

## 7. 验收标准

1. 给定一份 5 章以上的中文小说 txt 文件，CLI 工具无报错运行完毕并输出 `.yaml` 文件。
2. 输出 YAML 通过本项目 Schema 校验。
3. 全局角色表中的角色名称与各场 `characters` 字段一致（无别名混用）。
4. 每场 `slugline` 包含 内/外、地点、时间三个元素。
5. 场号从 1 起始、全局连续、不重复。
